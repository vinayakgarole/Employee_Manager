create database Employee_Manager;
use Employee_Manager;
create table EmployeeManager(id int (10) auto_increment primary key,
firstName varchar (20),
 lastName varchar (20),
 phoneNumber bigint (12),
 salary int (10));
 
  select * from EmployeeManager;